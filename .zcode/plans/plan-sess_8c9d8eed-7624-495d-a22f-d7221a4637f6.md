Fix fight readability while keeping the pixel rain engine as the renderer for everything on screen.

## 1. Characters stay rain-built (per your note)
- The fighter sprites are never drawn today — only the faint rain reveal shows. Keep the rain as the body, but make it actually readable: boost each fighter's focused rain layer (density 5000→8000, pixel size 1.5×, reveal 1.5×) and draw the current sprite frame as a **translucent pixel base (α≈0.5) beneath its rain**, so the character reads as an apparition formed by the rain — animation clearly visible, aesthetic intact.
- Feet-on-floor: landing currently puts the sprite's center at the platform line (half the body below the top). Change physics to land with feet on the surface (`y = platform.y − renderHeight/2`). Add a small pixel contact shadow on the slab under each fighter.

## 2. Zoomed camera for small dev windows
- Dynamic camera in `SmashStage.render`: follows the fighters' midpoint, zoom = clamp(width / (distance + 640), 1.25, 1.8) — in close when they fight, out when they separate; smooth-lerped. World (rain included) renders inside the camera transform; HUD stays screen-space. `smash.js` passes delta to render for the lerp.

## 3. Names + arrows, pixel-native
- Above each fighter: a bobbing **pixel-stepped arrow** (fillRect tiers, not a smooth path) + monospace name plate — ANDY green, DUMMY pink, ELISEO when swapped. Drawn in world space so it tracks and scales with the rain scene.

## 4. Swapping (question 1)
- The "Fighter: ANDY" button in the top bar already swaps. Relabel to "Swap fighter · C (ANDY)" and add the **C key** shortcut so it works even with a cramped window.

## 5. Hot-reload reliability
- `server.mjs` sends `Cache-Control: no-store` so refreshes always pick up fresh code (likely why changes seemed invisible after refresh).

## Verification
- 16 tests stay green. In-browser offscreen-render check: fighter pixels visible above the platform line, tags drawn, camera zoom engaged, walk frames changing on the rendered canvas, zero page errors.